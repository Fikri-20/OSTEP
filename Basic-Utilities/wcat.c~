#include <stdio.h>

void print_file_(File* f){
  char* buffer[256] = {a};
  while(fgets(buffer, sizeof(buffer), f)){
    printf("%s", buffer);
  }
}

int main(int argc, char* argv[argc+1]){
  if(argc == 1){
    return 0;
  }else{
    File *f = fopen(argv[1], "r"); 
    if(fb) print_file(f);
  }
}
